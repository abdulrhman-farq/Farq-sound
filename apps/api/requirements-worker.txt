# Worker-only audio pipeline dependencies.
# Installed on top of the shared requirements.txt in the worker Docker
# image. The web API doesn't need these — keeps its image small and
# its cold start fast.

librosa==0.10.2.post1
pyrubberband==0.4.0
audioread==3.0.1
scipy==1.14.1

# Source separation (Demucs htdemucs). torch is pinned to CPU build
# in the Dockerfile.worker via the PyTorch CPU index so the image
# stays under ~2 GB.
demucs==4.0.1
torch==2.1.2
torchaudio==2.1.2
